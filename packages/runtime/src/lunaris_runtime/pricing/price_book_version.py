# Bump whenever any rate in ``price_book_data`` changes — a build stamps this onto every cost row,
# so old costs stay pinned to the rates they were priced under (a later change never rewrites them).
PRICE_BOOK_VERSION = "2026-07-25"
