"""Lunaris Live — the second product: runtime-generated tutoring sessions over a concept graph.

Live may build on Studio's foundations; Studio must never import Live. That one-way boundary is
enforced by ``tests/test_product_boundary.py`` (Python) and an ESLint rule (web).
"""
